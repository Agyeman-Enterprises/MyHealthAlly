# Database Configuration
# Option 1: Supabase (Recommended for production/teams)
# Get connection string from: https://supabase.com/dashboard/project/[YOUR-PROJECT]/settings/database
# Use pooled connection for app: ?pgbouncer=true
# Use direct connection for migrations: remove ?pgbouncer=true
# DATABASE_URL="postgresql://postgres:[YOUR-PASSWORD]@db.[PROJECT-REF].supabase.co:6543/postgres?pgbouncer=true"

# Option 2: Local Docker (for offline development)
# Requires: docker-compose up -d
DATABASE_URL="postgresql://myhealthally:myhealthally_dev@localhost:5432/myhealthally?schema=public"

# JWT Authentication
JWT_SECRET="your-super-secret-jwt-key-change-in-production-min-32-chars"
JWT_EXPIRES_IN="15m"
JWT_REFRESH_SECRET="your-super-secret-refresh-key-change-in-production-min-32-chars"
JWT_REFRESH_EXPIRES_IN="7d"

# Redis Configuration
REDIS_HOST="localhost"
REDIS_PORT=6379

# Server Configuration
PORT=3000
NODE_ENV="development"
FRONTEND_URL="http://localhost:3001"

# Daily.co Video API
DAILY_API_KEY="placeholder-secret"
