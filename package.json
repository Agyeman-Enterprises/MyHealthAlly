{
  "name": "myhealthally-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "MyHealthAlly - Continuous Care App Monorepo",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm --filter backend dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=8.0.0"
  }
}

